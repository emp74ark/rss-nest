meta {
  name: Log in
  type: http
  seq: 1
}

post {
  url: {{backend}}/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "login": "{{username}}",
    "password": "{{password}}"
  }
}

settings {
  encodeUrl: true
}
